
TODO

     * Add support for anonymous and anyone rights
     * Add Search for User functionality (linking to ldapquery, with
       javascript)
     * Check if user already has mailbox
     * Add user checking method that checks for existence of user's
       mailbox
     * Fix for Alternate namespace
     * Update all == adds new username as well
